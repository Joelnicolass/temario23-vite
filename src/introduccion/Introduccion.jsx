// COMPONENTES
// Son la base de React, son funciones o clases que tienen su contraparte en el DOM, es decir, son funciones o clases que retornan un elemento de React -que a su vez es un objeto que representa un elemento del DOM-

// STATELESS COMPONENTS

// Son componentes que no tienen estado, no tienen ciclo de vida, no tienen eventos, no tienen nada, solo son una función que renderiza datos.
//---------------------------------------------------------------------------
// STATEFUL COMPONENTS

// Son componentes que tienen estado, tienen ciclo de vida, tienen eventos, son clases que extienden de Component o funciones que uitilizan hooks, ademas de renderizar datos.

// EXPORT E IMPORT

// Para exportar un componente se utiliza la palabra reservada export, y para importar un componente se utiliza la palabra reservada import.

/* 

    EXPORTACION NOMBRADA
    export const variable = "valor";
    export const funcion = () => {};

    import { variable, funcion } from "./ruta";


    EXPORTACION POR DEFECTO
    export default variable;

    import variable from "./ruta";

*/
